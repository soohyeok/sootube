header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                i.fab.fa-youtube
        .header__column
            form(action=routes.search, method="get")
                input(type="text", placeholder="Search by term...", name="term")
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.signUp) Sign Up
                    li
                        a(href=routes.login) Log In
                else 
                    li
                        a(href=`/videos${routes.videoUpload}`) Upload
                    li
                        a(href=routes.profile) Profile
                    li
                        a(href=routes.logout) Log Out

                        