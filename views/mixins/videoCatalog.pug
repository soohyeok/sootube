mixin videoCatalog(video = {})
    .videoCatalog
        a(href=routes.videoDetail(video.id))
            video.videoCatalog__thumbnail(src=video.videoFile, controls=true)
            h4.videoCatalog__title=video.title
            h6.videoCatalog__views=video.views