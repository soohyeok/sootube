extends layouts/main
include mixins/videoCatalog

block content
    .home-videos
        each item in videosData
            +videoCatalog({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })