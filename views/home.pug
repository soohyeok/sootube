extends layouts/main.pug
include mixins/videoCatalog

block content
    .videos
        each vid in videosData
            +videoCatalog({
                id: vid.id,
                title: vid.title,
                views: vid.views,
                videoFile: vid.fileUrl
            })